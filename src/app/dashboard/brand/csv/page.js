import { UserCreateViews } from 'src/sections/brand/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Review System: Import CSV file',
};

export default function UserCreatePage() {
  return <UserCreateViews />;
}
