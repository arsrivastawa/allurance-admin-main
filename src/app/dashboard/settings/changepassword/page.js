import { ProductCreateView } from 'src/sections/settings/changepassword';

export const metadata = {
    title: 'Dashboard: Update Password',
};

export default function ProductCreatePage() {
    return <ProductCreateView />;
}
