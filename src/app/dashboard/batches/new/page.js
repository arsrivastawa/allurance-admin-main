import { ProductCreateView } from 'src/sections/batches/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Add Batch',
};

export default function ProductCreatePage() {
  return <ProductCreateView />;
}
