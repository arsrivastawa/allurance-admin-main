import { ProductCreateView } from 'src/sections/findreplication/view';

export const metadata = {
  title: 'Dashboard: Add New replication',
};

export default function ProductCreatePage() {
  return <ProductCreateView />;
}
